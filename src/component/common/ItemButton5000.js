import {Text,Button} from '../common';
import {View,StyleSheet} from 'react-native';

export default ItemButton = ({styles,title}) => {

}

const styles = StyleSheet.create({
    container : {
        flex : 1,
    }
})